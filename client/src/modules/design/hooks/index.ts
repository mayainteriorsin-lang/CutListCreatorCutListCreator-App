/**
 * Design Module Hooks - Barrel Export
 *
 * Custom hooks that wrap the design store for convenient access.
 * These hooks provide optimized access to specific slices of state
 * and utility functions for common operations.
 */

export { useShapes } from "./useShapes";
export { useHistory } from "./useHistory";
export { useCanvas } from "./useCanvas";
export { useKeyboardShortcuts } from "./useKeyboardShortcuts";
export { useMouseHandlers } from "./useMouseHandlers";
